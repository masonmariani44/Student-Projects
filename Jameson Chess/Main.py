from board import Board

my_board = Board()

current_turn = "w"

whites 

while True:
    
    # make a move
        # look at the moving piece
        # get its target destination
            # check if move is valid
            # if move is valid !! for that piece !!
                # move piece to that position, update board
            # if move isnt valid
                # ask again for a new move
    
    # switch current_turn to be opposite player
    # do next loop